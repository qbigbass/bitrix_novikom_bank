import type { DefaultSeparators, JSDefaultClasses } from "../interfaces";

export const JS_DEFAULT_CLASSES: JSDefaultClasses = {
	slider: '.js-slider',
	slide: '.js-swiper-slide',
	wrapper: '.js-swiper-wrapper',
	controls: '.js-swiper-controls',
	pagination: '.js-swiper-pagination',
	nav: '.js-swiper-nav',
	prevEl: '.js-swiper-prev',
	nextEl: '.js-swiper-next',
}

export const DEFAULT_SEPARATORS: DefaultSeparators = {
	mediaQuery: ':',
	mediaItem: ','
}