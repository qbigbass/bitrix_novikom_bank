import type { JSDefaultClasses, DefaultActiveClasses } from "@components/ui/ATabs/interfaces";

export const JS_DEFAULT_CLASSES: JSDefaultClasses = {
	root: '.js-a-tabs',
	tab: '.js-a-tab',
	swiper: '.js-a-tab-swiper',
	panels: '.js-a-tab-panels',
	panel: '.js-a-tab-panel',
}

export const DEFAULT_ACTIVE_CLASSES: DefaultActiveClasses = {
	active: 'is-active',
	hidden: 'is-hidden',
}