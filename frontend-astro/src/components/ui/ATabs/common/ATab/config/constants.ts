import type { JSClasses, ActiveClasses } from "@components/ui/ATabs/common/ATab/interfaces";

export const DEFAULT_JS_CLASSES: JSClasses = {
	root: '.js-a-tab'
}

export const DEFAULT_ACTIVE_CLASSES: ActiveClasses = {
	active: 'is-active'
}